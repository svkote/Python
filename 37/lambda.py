# Анонимные функции могут содержать лишь одно выражение, но и выполняются они быстрее. Анонимные функции создаются с
# помощью инструкции lambda.

func = lambda x, y: x + y

func(1, 2)

func('a', 'b')

(lambda x, y: x + y)(1, 2)

(lambda x, y: x + y)('a', 'b')
